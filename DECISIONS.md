# 长期决策

## 2026-09-12：产品边界与验证门槛

- 产品 Subloom，面向 FCP、Apple Silicon、本地简体字幕。优先原生 Workflow Extension，不做网页 UI。
- 理想流程为选范围、打开面板、生成、回到当前原始时间线。不能默认新建字幕版项目；替代流程必须以实际步骤和实测证据交用户选择。
- 先验证读取、音频映射、角色及写回闭环，再实现字幕校对、词库、参考脚本、模型管理等完整 UI。
- 原生 Caption 优先验证；Title 是待比较选项，尚未选为产品输出。
- FCP 音频以角色、子角色、组件建模，不套用 Resolve 音轨。未知可听状态不得静默解释成全部可听。
- 时间映射采用有理数；区分项目起始时间码、片段源时间、父级 offset、识别相对时间；不按文字长度伪造对齐。
- Qwen3-ASR 0.6B 为 MVP 优先；脚本只辅助，以音频为准。后续复用独立文字处理规则，保留数值与技术符号。
- Subloom 独立 Git 仓库与数据。首次离线探针允许只读借用 VinciSub 已安装解释器和模型，不能成为产品运行依赖；未复制其宿主实现。
- 未采用 XML 全项目替换：FCP 的项目级替换不是已验证的原时间线字幕增量更新。

## 2026-09-12：更名为 SubPop

- 用户将产品名从 Subloom 改为 SubPop；所有新应用显示名、探针类名、bundle identifier 和日志目录使用 SubPop。
- 当前代码库物理目录 `/Users/chokamin/Desktop/Subloom`、`.subloom` 构建缓存及历史 FCP 测试资源库暂保留，避免移动正在使用的工作区及破坏历史证据；这些不是产品显示名称。
- 用户已明确允许安装并启动只读原生探针；此次更名沿用该授权。

## 2026-09-12：仓库目录更名

- 用户要求仓库目录也改名；实际目录已迁移至 `/Users/chokamin/Desktop/SubPop`，保留原 Git 历史，不创建第二份仓库。
- 旧 Subloom 路径暂为兼容链接，供当前会话及历史测试媒体引用使用。`.subloom` 和历史测试项目名不随此次迁移改写。
